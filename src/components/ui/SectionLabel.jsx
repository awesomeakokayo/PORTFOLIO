import React from 'react';

export const SectionLabel = ({ children }) => (
  <p className="font-body text-[11px] font-medium tracking-[0.15em] 
                text-accent uppercase mb-4">
    {children}
  </p>
);

export default SectionLabel;
